import { Component, linkedSignal, signal } from '@angular/core';

@Component({
  selector: 'app-lsignals',
  imports: [],
  templateUrl: './lsignals.html',
  styleUrl: './lsignals.css',
})
export class Lsignals {

//   quantitySignal = signal(1);
//   price = 10;

// //   total = linkedSignal(() => {
// //   return this.quantitySignal() * this.price;
// //   }
// // )

// //   total = linkedSignal(() => {
// //   source : this.quantitySignal,
// //   computation: () => this.quantitySignal() * this.price;
// //   }
// // )

//   calculate(){
//     this.quantitySignal.set(5);
//   }

}

